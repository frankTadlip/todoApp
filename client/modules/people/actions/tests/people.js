const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../people';

describe('people.actions.people', () => {
  it('should do something');
});
